apiVersion: v1
kind: Pod
metadata:
  name: env-variables
  labels:
    project: roboshop
    environment: dev
spec:
  containers:
  - name: nginx
    image: nginx
    envFrom:
      - configMapRef:
          name: configmap

