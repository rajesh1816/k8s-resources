apiVersion: v1
kind: Pod
metadata:
  name: labels
  labels:
    purpose: labels-demo
    project: roboshop
    environment: dev
    tier: backend
spec:
  containers:
  - name: catalogue-nginx-container
    image: nginx
